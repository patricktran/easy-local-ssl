import React from "react";
import ReactDOM from "react-dom";

const Home = () => {
  return <h1>easy-local-ssl with React + Webpack</h1>;
};

ReactDOM.render(<Home />, document.getElementById("app"));
